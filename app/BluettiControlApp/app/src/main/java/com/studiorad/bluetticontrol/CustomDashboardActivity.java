package com.studiorad.bluetticontrol;

public class CustomDashboardActivity extends ModernMainActivity {
    // Classe mantida apenas para compatibilidade com versões antigas do manifesto.
    // O layout ativo agora fica em ModernMainActivity, sem gráficos.
}
